// App configuration

export const config = {
  // Polling interval in milliseconds for auto-refreshing data
  // Set to 0 to disable polling
  pollingInterval: 30000, // 30 seconds

  // Show toast notifications when new data is detected
  showPollingNotifications: true,
};
